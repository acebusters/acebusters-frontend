// import { fromJS } from 'immutable';
// import { makeSelectRegisterPageDomain } from '../selectors';

// const selector = makeSelectRegisterPageDomain();

describe('makeSelectRegisterPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
